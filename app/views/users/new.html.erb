<div class="hero">
	<iframe width="100%" height="739px" src="https://www.youtube.com/embed/pB0mHOTM1FM" frameborder="0" allow="autoplay; encrypted-media" allowfullscreen id="bg" ></iframe>
  <p class="large brandon">LOCALHOOPS IS COMING</p>
  <p class="small brandon">LOCAL TRAINERS,PLAYERS AND COURTS</p>
</div>

<div class="form-wrap clearfix <%= "ended" if Rails.application.config.ended %>">
  <% if Rails.application.config.ended %>
    <%= render "users/campaign_ended" %>
  <% else %>
    <%= render "users/campaign_ongoing" %>
  <% end %>
</div>

